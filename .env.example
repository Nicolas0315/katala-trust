# Optional Anthropic SDK path used by ProfilingEngine adapters
# ANTHROPIC_API_KEY=

# Gateway peer auth (required unless KATALA_GATEWAY_ALLOW_TAILNET_ONLY=1)
# KATALA_GATEWAY_PEER_TOKENS=token-a,token-b
# KATALA_GATEWAY_PORT=18789
# KATALA_GATEWAY_ALLOW_TAILNET_ONLY=0
